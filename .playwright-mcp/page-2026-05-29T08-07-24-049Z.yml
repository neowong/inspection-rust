- generic [ref=e2]: "import { useState, useEffect } from \"react\"; import { invoke } from \"@tauri-apps/api/core\"; import type { InspectionTemplate, CommandPool } from \"../types\"; import Toolbar from \"../components/Toolbar\"; import SearchInput from \"../components/SearchInput\"; import DataTable from \"../components/DataTable\"; import Modal from \"../components/Modal\"; import ContextMenu, { type ContextMenuItem } from \"../components/ContextMenu\"; import Button from \"../components/ui/Button\"; import Input, { Select } from \"../components/ui/Input\"; const VENDORS = [\"H3C\", \"鍗庝负\", \"鎬濈\", \"閿愭嵎\"]; const CATEGORIES = [\"version\", \"clock\", \"cpu\", \"memory\", \"hardware\", \"interface\", \"vlan\", \"log\", \"protocol\", \"general\"]; interface TemplateForm { name: string; vendor: string; model: string; device_type: string; description: string; command_ids: number[]; } const EMPTY_TEMPLATE_FORM: TemplateForm = { name: \"\", vendor: \"H3C\", model: \"\", device_type: \"\", description: \"\", command_ids: [], }; interface CommandForm { vendor: string; command: string; description: string; category: string; } const EMPTY_COMMAND_FORM: CommandForm = { vendor: \"H3C\", command: \"\", description: \"\", category: \"general\", }; export default function TemplatesPage() { // Template state const [templates, setTemplates] = useState<InspectionTemplate[]>([]); const [templateSearch, setTemplateSearch] = useState(\"\"); const [templateVendor, setTemplateVendor] = useState(\"\"); const [selectedTemplate, setSelectedTemplate] = useState<InspectionTemplate | null>(null); const [ctxMenu, setCtxMenu] = useState<{ x: number; y: number } | null>(null); const [templateModal, setTemplateModal] = useState(false); const [editingTemplate, setEditingTemplate] = useState<InspectionTemplate | null>(null); const [templateForm, setTemplateForm] = useState<TemplateForm>(EMPTY_TEMPLATE_FORM); const [confirmDeleteTemplate, setConfirmDeleteTemplate] = useState<number | null>(null); const [saving, setSaving] = useState(false); // Command pool state const [commands, setCommands] = useState<CommandPool[]>([]); const [cmdSearch, setCmdSearch] = useState(\"\"); const [cmdVendor, setCmdVendor] = useState(\"\"); const [cmdCategory, setCmdCategory] = useState(\"\"); const [cmdModal, setCmdModal] = useState(false); const [cmdForm, setCmdForm] = useState<CommandForm>(EMPTY_COMMAND_FORM); const loadTemplates = () => { invoke<InspectionTemplate[]>(\"list_templates\", { vendor: templateVendor || undefined }) .then(setTemplates).catch(console.error); }; const loadCommands = () => { invoke<CommandPool[]>(\"list_commands\", { vendor: cmdVendor || undefined, category: cmdCategory || undefined, }).then(setCommands).catch(console.error); }; useEffect(() => { loadTemplates(); }, [templateVendor]); useEffect(() => { loadCommands(); }, [cmdVendor, cmdCategory]); // Filter templates const filteredTemplates = templates.filter((t) => !templateSearch || t.name.toLowerCase().includes(templateSearch.toLowerCase()) ); // Filter commands const filteredCommands = commands.filter((c) => !cmdSearch || c.command.toLowerCase().includes(cmdSearch.toLowerCase()) || (c.description && c.description.toLowerCase().includes(cmdSearch.toLowerCase())) ); // Template handlers const openAddTemplate = () => { setEditingTemplate(null); setTemplateForm(EMPTY_TEMPLATE_FORM); setTemplateModal(true); }; const openEditTemplate = (t: InspectionTemplate) => { setEditingTemplate(t); setTemplateForm({ name: t.name, vendor: t.vendor, model: t.model || \"\", device_type: t.device_type || \"\", description: t.description || \"\", command_ids: t.config?.command_ids || [], }); setTemplateModal(true); }; const handleSaveTemplate = () => { const data: Record<string, unknown> = { name: templateForm.name, vendor: templateForm.vendor, config: JSON.stringify({ command_ids: templateForm.command_ids }), }; if (templateForm.model) data.model = templateForm.model; if (templateForm.device_type) data.device_type = templateForm.device_type; if (templateForm.description) data.description = templateForm.description; setSaving(true); const promise = editingTemplate ? invoke<InspectionTemplate>(\"update_template\", { templateId: editingTemplate.id, data }) : invoke<InspectionTemplate>(\"create_template\", { data }); promise .then(() => { setTemplateModal(false); loadTemplates(); }) .catch((e) => alert(`淇濆瓨澶辫触: ${e}`)) .finally(() => setSaving(false)); }; const handleDeleteTemplate = (id: number) => { invoke<void>(\"delete_template\", { templateId: id }) .then(() => { setConfirmDeleteTemplate(null); loadTemplates(); }) .catch(console.error); }; const handleAutoGenerate = (t: InspectionTemplate) => { invoke<{ command_ids: number[] }>(\"auto_generate_template\", { vendor: t.vendor, model: t.model || undefined, device_type: t.device_type || undefined, }).then(() => loadTemplates()).catch(console.error); }; // Command handlers const handleSaveCommand = () => { invoke<CommandPool>(\"create_command\", { data: { ...cmdForm } }) .then(() => { setCmdModal(false); setCmdForm(EMPTY_COMMAND_FORM); loadCommands(); }) .catch(console.error); }; const handleTemplateCtx = (e: React.MouseEvent, t: InspectionTemplate) => { e.preventDefault(); setSelectedTemplate(t); setCtxMenu({ x: e.clientX, y: e.clientY }); }; const ctxItems: ContextMenuItem[] = selectedTemplate ? [ { label: \"缂栬緫\", action: () => openEditTemplate(selectedTemplate) }, { label: \"\", separator: true }, { label: \"鑷姩鐢熸垚鍛戒护\", action: () => handleAutoGenerate(selectedTemplate) }, { label: \"\", separator: true }, { label: \"鍒犻櫎\", danger: true, action: () => setConfirmDeleteTemplate(selectedTemplate.id) }, ] : []; return ( <div className=\"space-y-6\"> <div> <h1 className=\"text-2xl font-bold text-[hsl(var(--text-primary))]\">宸℃妯℃澘</h1> <p className=\"text-sm text-[hsl(var(--text-secondary))] mt-1\">绠＄悊宸℃妯℃澘鍜屽懡浠ゅ簱</p> </div> {/* Templates Section */} <div> <h2 className=\"text-lg font-semibold text-[hsl(var(--text-primary))] mb-3\">妯℃澘鍒楄〃</h2> <Toolbar> <Button onClick={openAddTemplate} size=\"sm\">娣诲姞妯℃澘</Button> <Select className=\"w-28\" value={templateVendor} onChange={(e) => setTemplateVendor(e.target.value)}> <option value=\"\">鍏ㄩ儴鍘傚晢</option> {VENDORS.map((v) => <option key={v} value={v}>{v}</option>)} </Select> <SearchInput value={templateSearch} onChange={setTemplateSearch} placeholder=\"鎼滅储妯℃澘...\" /> </Toolbar> <DataTable<InspectionTemplate> columns={[ { key: \"name\", header: \"鍚嶇О\", render: (r) => r.name }, { key: \"vendor\", header: \"鍘傚晢\", render: (r) => r.vendor }, { key: \"command_count\", header: \"鍛戒护鏁�\", width: \"80px\", render: (r) => String((r.config?.command_ids || []).length), }, { key: \"description\", header: \"鎻忚堪\", render: (r) => r.description || \"-\" }, { key: \"updated_at\", header: \"鏇存柊鏃堕棿\", render: (r) => new Date(r.updated_at).toLocaleString(\"zh-CN\"), }, ]} data={filteredTemplates} rowKey={(r) => r.id} onRowClick={(r) => setSelectedTemplate(r)} onRowDoubleClick={(r) => openEditTemplate(r)} onContextMenu={handleTemplateCtx} selectedKey={selectedTemplate?.id} emptyText=\"鏆傛棤妯℃澘\" /> </div> {/* Command Pool Section */} <div> <h2 className=\"text-lg font-semibold text-[hsl(var(--text-primary))] mb-3\">鍛戒护搴�</h2> <Toolbar> <Button onClick={() => { setCmdForm(EMPTY_COMMAND_FORM); setCmdModal(true); }} size=\"sm\">娣诲姞鍛戒护</Button> <Select className=\"w-28\" value={cmdVendor} onChange={(e) => setCmdVendor(e.target.value)}> <option value=\"\">鍏ㄩ儴鍘傚晢</option> {VENDORS.map((v) => <option key={v} value={v}>{v}</option>)} </Select> <Select className=\"w-28\" value={cmdCategory} onChange={(e) => setCmdCategory(e.target.value)}> <option value=\"\">鍏ㄩ儴鍒嗙被</option> {CATEGORIES.map((c) => <option key={c} value={c}>{c}</option>)} </Select> <SearchInput value={cmdSearch} onChange={setCmdSearch} placeholder=\"鎼滅储鍛戒护...\" /> </Toolbar> <DataTable<CommandPool> columns={[ { key: \"vendor\", header: \"鍘傚晢\", width: \"80px\", render: (r) => r.vendor }, { key: \"command\", header: \"鍛戒护\", render: (r) => <code className=\"text-xs bg-[hsl(var(--bg-hover))] px-1.5 py-0.5 rounded\">{r.command}</code> }, { key: \"description\", header: \"鎻忚堪\", render: (r) => r.description || \"-\" }, { key: \"category\", header: \"鍒嗙被\", width: \"100px\", render: (r) => r.category || \"-\" }, ]} data={filteredCommands} rowKey={(r) => r.id} emptyText=\"鏆傛棤鍛戒护\" /> </div> <ContextMenu items={ctxItems} visible={ctxMenu !== null} x={ctxMenu?.x ?? 0} y={ctxMenu?.y ?? 0} onClose={() => setCtxMenu(null)} /> {/* Template Modal */} <Modal open={templateModal} title={editingTemplate ? \"缂栬緫妯℃澘\" : \"娣诲姞妯℃澘\"} width=\"max-w-lg\" onClose={() => setTemplateModal(false)} footer={ <div className=\"flex gap-2\"> <Button variant=\"secondary\" onClick={() => setTemplateModal(false)}>鍙栨秷</Button> <Button onClick={handleSaveTemplate} loading={saving}>{editingTemplate ? \"淇濆瓨\" : \"娣诲姞\"}</Button> </div> } > <div className=\"space-y-3\"> <div className=\"grid grid-cols-2 gap-3\"> <div> <label className=\"block text-xs font-medium text-[hsl(var(--text-secondary))] mb-1\">鍚嶇О</label> <Input value={templateForm.name} onChange={(e) => setTemplateForm({ ...templateForm, name: e.target.value })} /> </div> <div> <label className=\"block text-xs font-medium text-[hsl(var(--text-secondary))] mb-1\">鍘傚晢</label> <Select value={templateForm.vendor} onChange={(e) => setTemplateForm({ ...templateForm, vendor: e.target.value })}> {VENDORS.map((v) => <option key={v} value={v}>{v}</option>)} </Select> </div> </div> <div className=\"grid grid-cols-2 gap-3\"> <div> <label className=\"block text-xs font-medium text-[hsl(var(--text-secondary))] mb-1\">鍨嬪彿锛堝彲閫夛級</label> <Input value={templateForm.model} onChange={(e) => setTemplateForm({ ...templateForm, model: e.target.value })} /> </div> <div> <label className=\"block text-xs font-medium text-[hsl(var(--text-secondary))] mb-1\">璁惧绫诲瀷锛堝彲閫夛級</label> <Input value={templateForm.device_type} onChange={(e) => setTemplateForm({ ...templateForm, device_type: e.target.value })} /> </div> </div> <div> <label className=\"block text-xs font-medium text-[hsl(var(--text-secondary))] mb-1\">鎻忚堪锛堝彲閫夛級</label> <Input value={templateForm.description} onChange={(e) => setTemplateForm({ ...templateForm, description: e.target.value })} /> </div> <div> <label className=\"block text-xs font-medium text-[hsl(var(--text-secondary))] mb-2\">閫夋嫨鍛戒护</label> <div className=\"max-h-48 overflow-y-auto border border-[hsl(var(--border))] rounded-md p-2 space-y-1\"> {commands.length === 0 && <p className=\"text-xs text-[hsl(var(--text-tertiary))]\">鏆傛棤鍛戒护锛岃鍏堝湪鍛戒护搴撲腑娣诲姞</p>} {commands.map((cmd) => { const checked = templateForm.command_ids.includes(cmd.id); return ( <label key={cmd.id} className=\"flex items-center gap-2 cursor-pointer hover:bg-[hsl(var(--bg-hover))] rounded px-1 py-0.5\"> <input type=\"checkbox\" checked={checked} onChange={() => { setTemplateForm({ ...templateForm, command_ids: checked ? templateForm.command_ids.filter((id) => id !== cmd.id) : [...templateForm.command_ids, cmd.id], }); }} className=\"accent-[hsl(var(--accent))]\" /> <span className=\"text-xs\"> <code className=\"bg-[hsl(var(--bg-hover))] px-1 rounded\">{cmd.command}</code> {cmd.description && <span className=\"text-[hsl(var(--text-tertiary))] ml-1\">鈥� {cmd.description}</span>} </span> </label> ); })} </div> </div> </div> </Modal> {/* Delete Template Confirm */} <Modal open={confirmDeleteTemplate !== null} title=\"纭鍒犻櫎\" width=\"max-w-sm\" onClose={() => setConfirmDeleteTemplate(null)} footer={ <div className=\"flex gap-2\"> <Button variant=\"secondary\" onClick={() => setConfirmDeleteTemplate(null)}>鍙栨秷</Button> <Button variant=\"danger\" onClick={() => handleDeleteTemplate(confirmDeleteTemplate!)}>鍒犻櫎</Button> </div> } > <p>纭畾瑕佸垹闄ゆ妯℃澘鍚楋紵姝ゆ搷浣滀笉鍙仮澶嶃€�</p> </Modal> {/* Command Modal */} <Modal open={cmdModal} title=\"娣诲姞鍛戒护\" width=\"max-w-md\" onClose={() => setCmdModal(false)} footer={ <div className=\"flex gap-2\"> <Button variant=\"secondary\" onClick={() => setCmdModal(false)}>鍙栨秷</Button> <Button onClick={handleSaveCommand}>娣诲姞</Button> </div> } > <div className=\"space-y-3\"> <div> <label className=\"block text-xs font-medium text-[hsl(var(--text-secondary))] mb-1\">鍘傚晢</label> <Select value={cmdForm.vendor} onChange={(e) => setCmdForm({ ...cmdForm, vendor: e.target.value })}> {VENDORS.map((v) => <option key={v} value={v}>{v}</option>)} </Select> </div> <div> <label className=\"block text-xs font-medium text-[hsl(var(--text-secondary))] mb-1\">鍛戒护鏂囨湰</label> <Input value={cmdForm.command} onChange={(e) => setCmdForm({ ...cmdForm, command: e.target.value })} placeholder=\"display version\" /> </div> <div> <label className=\"block text-xs font-medium text-[hsl(var(--text-secondary))] mb-1\">鎻忚堪锛堝彲閫夛級</label> <Input value={cmdForm.description} onChange={(e) => setCmdForm({ ...cmdForm, description: e.target.value })} /> </div> <div> <label className=\"block text-xs font-medium text-[hsl(var(--text-secondary))] mb-1\">鍒嗙被</label> <Select value={cmdForm.category} onChange={(e) => setCmdForm({ ...cmdForm, category: e.target.value })}> {CATEGORIES.map((c) => <option key={c} value={c}>{c}</option>)} </Select> </div> </div> </Modal> </div> ); }"