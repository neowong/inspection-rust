export const VENDORS = ["H3C", "华为", "思科", "锐捷", "其它"] as const;

export const CATEGORIES = [
  "version", "clock", "cpu", "memory", "hardware",
  "interface", "vlan", "log", "protocol", "wireless", "general",
] as const;
